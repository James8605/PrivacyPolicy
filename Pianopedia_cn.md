# 隐私政策

**应用名称：** Pianopedia
**生效日期：** 2026-05-09
**最后更新：** 2026-05-09

本政策说明 Pianopedia（以下简称「本应用」）如何处理你的数据。本应用无需注册账号，并默认将数据保留在你的设备本地。本政策适用于通过 App Store 分发的 iOS 应用。

---

## 摘要

- 本应用无需注册或登录账号。
- 本应用不收集分析数据、广告标识符、位置信息或通讯录。
- 麦克风仅用于检测你弹奏的音高，音频在设备本地实时处理，不会被录制、保存或上传。
- 相机与相册仅用于导入你选择扫描的乐谱。
- 云端识谱为可选功能，需用户主动开启。开启后，你选择扫描的乐谱图片会使用你自行提供的 API Key 发送至 LLM Provider API。本应用不通过我们运营的任何服务器中转此数据。
- 你的曲库、练习历史与设置均保存在设备本地。

---

## 本应用涉及的数据

### 1. 麦克风音频（实时处理，不保留）

当你开始一次练习时，本应用通过麦克风检测你弹奏的音高与节奏。音频样本在设备本地以标准音高检测算法处理后立即丢弃。本应用不会将音频写入存储，也不会将音频上传至任何位置。

### 2. 照片与相机输入

你可以通过相机、相册或 iOS 文档扫描仪导入乐谱。本应用仅读取你在导入操作中明确选择的图片。导入的图片仅在提取出可播放曲谱所需的时间内保留，得到的曲谱随后保存在你设备本地的曲库中。

### 3. 云端识谱（可选，需主动开启）

设备本地的识别器可在无网络的情况下识别简谱。对于五线谱与复杂排版，本应用提供一条可选的云端识谱通道，使用 LLM Provider 的 API。

要使用云端识谱，你需要：

1. 自行从 LLM Provider 获取 API Key。
2. 在「设置 -> 云端识谱」中填入该 Key。

启用云端识谱后，当你触发扫描时：

- 所选乐谱图片在本地缩放至约 1024 像素并以 JPEG 压缩。
- 图片与识别提示词由你的设备直接发送至 LLM Provider 的 API 端点，并使用你的 API Key 进行身份认证。
- API 返回的结构化曲谱由本应用在本地解析并保存至你的曲库。

本应用不运营任何中间服务器。LLM Provider 对该请求的处理受其自身的服务条款与隐私政策约束。本应用无法控制 LLM Provider 如何处理请求数据，详情请查阅 LLM Provider 的相关文档。

若未启用云端识谱（未保存 API Key），不会有任何图片离开你的设备。

### 4. API Key 的存储

如果你选择启用云端识谱，你的 LLM Provider API Key 会保存在设备的 iOS Keychain 中（`kSecClassGenericPassword`，访问级别 `kSecAttrAccessibleAfterFirstUnlock`，未启用同步标记）。该 Key 不会进入 iCloud Keychain 同步，不会发送至本应用的任何服务器（我们未运营任何此类服务器），仅在你触发扫描时发送至 LLM Provider 的 API 端点。你可随时在「设置 -> 云端识谱 -> 移除 Key」中删除该 Key。

### 5. 曲库与练习历史

你导入或编辑的乐谱、练习历史（每次的得分、节奏准确率、完成度、逐音结果、时长、日期）、偏好设置（主题、语言、记谱方式）以及引导状态，均保存在本应用的本地沙盒中。这些数据不会被上传至本应用或任何第三方。

如果你为设备启用了 iCloud 备份，操作系统可能会将本应用的容器纳入由 Apple 管理的加密设备备份中。本应用无法控制或读取此类备份。

### 6. 网络请求

仅当你的操作需要时，本应用才会发起对外网络请求：

- **发现 / 搜索**：在应用内搜索曲谱时，会向公开乐谱站点（例如 Mutopia Project、piano-midi.de）以及你在「设置」中粘贴的自定义曲库 URL 发起 HTTP 请求。请求仅包含你的搜索关键词与标准 HTTP 头。
- **云端识谱**：如上文所述。需要你的 API Key，仅在你以云端方式扫描乐谱时触发。
- **曲谱下载**：当你从搜索结果导入一首作品时，本应用会从结果中列出的公开 URL 下载对应的 MIDI / MusicXML / JSON 文件。

本应用不会向任何分析、广告或遥测端点发送请求。

---

## 权限说明

| 权限   | 用途                                                 |
|--------|------------------------------------------------------|
| 麦克风 | 在练习时实时检测你的演奏音高与节奏。                 |
| 相机   | 拍摄乐谱以便导入。                                   |
| 相册   | 在导入已有乐谱时读取你所选的照片。                   |

你可随时在 **iOS「设置」-> Pianopedia** 中撤销以上任一权限。撤销后，本应用在不依赖该权限的功能下仍可继续使用。

---

## 我们不收集的数据

- 我们不收集或存储用户账号、姓名、邮箱地址或电话号码。
- 我们不收集广告设备标识符（IDFA）、位置数据、通讯录、日历或健康数据。
- 我们不集成任何第三方分析、崩溃上报或广告 SDK。
- 我们不会跨应用或跨网站对你进行追踪。

---

## 儿童

本应用面向广泛人群（包括儿童），且不会向任何用户（包括 13 岁以下儿童）收集个人信息。麦克风仅在练习时用于设备本地的音高检测，音频从不会被录制或传输。

如你（或你的孩子）选择启用云端识谱，请注意：发送至 LLM API 的请求受 LLM Provider 自身的条款约束；建议由家长或监护人代为配置和管理 API Key。

---

## 数据保留与删除

本应用所管理的全部数据均保存在你设备上的本地容器内。如需删除全部应用数据，请将 Pianopedia 从设备上卸载。在「设置 -> 云端识谱」中移除 API Key 即会从 Keychain 中删除该 Key。

---

## 安全

- 已保存的 LLM API Key 存放于 iOS Keychain。
- 与曲谱站点和 LLM API 的网络请求均通过 HTTPS。
- 音频处理与评分完全在设备本地完成。

任何系统都无法保证绝对安全，但我们在凭据存储与网络传输方面遵循标准 iOS 实践。

---

## 政策变更

如本应用的数据处理方式发生变化，我们将更新本政策并修改上方的「最后更新」日期。重大变更也会在 App Store 的版本更新说明中体现。

---

## 联系方式

如对本政策或本应用的隐私实践有任何疑问，请联系：

**邮箱：** zhangyang8605@gmail.com
